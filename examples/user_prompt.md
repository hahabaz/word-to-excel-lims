# Example invocation

请使用 `word-to-excel-lims` skill，将我上传的 Word 原始记录模板转换为 Excel 2019 兼容的 `.xlsx` 模板。

要求：
- 保留 Word 中肉眼可见的标题、字段、表格、横线、竖线、签字栏和页码；
- 使用等宽基础列 + 合并单元格重建版式；
- 不使用控件、文本框、形状或图片化表格；
- Word 勾选框统一转为 `□`；
- Excel 网格线保持开启；
- 打印页数尽量与 Word 打印预览一致；
- 生成后运行结构稳定性自检并给出简短 QA 摘要。
